uses
	Core;

pkg_config(LINUX) "pkg-config --cflags libsecret-1";

pkg_config(LINUX) "pkg-config --cflags glib-2.0";

file
	SecretStorage.h,
	SecretStorage.cpp,
	"Platform Specific" readonly separator,
	LinuxStorage.cpp,
	Win32Storage.cpp,
	MacOSStorage.cpp,
	Docs readonly separator,
	src.tpp,
	Info readonly separator,
	Copying;

mainconfig
	"" = "";

